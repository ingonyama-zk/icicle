name: Build Dev

on:
  pull_request:
    branches:
      - dev
    paths:
      - icicle/**
      - src/**
      - Cargo.toml
      - build.rs

env:
  CARGO_TERM_COLOR: always
  ARCH_TYPE: sm_70

jobs:
  build-linux:
    runs-on: [self-hosted, Linux, X64, icicle]
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v3
    - name: Build
      run: cargo build --release --verbose
      
  
  build-windows:
    runs-on: windows-2022
    steps:     
    - name: Checkout Repo
      uses: actions/checkout@v3
    - name: Download and Install Cuda
      uses: Jimver/cuda-toolkit@v0.2.11
      with:
        cuda: '12.0.0'
        method: 'network'
        # https://developer.download.nvidia.com/compute/cuda/repos/windows/x86_64/version_12.1.0.json
        sub-packages: '["cudart", "nvcc", "nvidia_driver", "cccl"]'
    - name: Build
      run: cargo build --release --verbose