use icicle_utils::*;

#[allow(non_snake_case)]
fn main() {
    // for cuda debug, run cargo build; then in vs code launch cuda debug
    //================================================================
    // let mut intoo = [Scalar::one(), Scalar::one() , Scalar::zero()];
    // let expected = [Scalar::one(), Scalar::zero() , Scalar::zero()];
    // mult_sc_vec(&mut intoo , &expected , 0);
    // assert_eq!(intoo, expected);

    // let mut intoo = [Point::one(), Point::one() , Point::zero()];
    // let scalars = [Scalar::one(), Scalar::zero() , Scalar::zero()];
    // let expected = [Point::one(), Point::zero() , Point::zero()];
    // multp_vec(&mut intoo , &scalars , 0);
    // assert_eq!(intoo, expected);

    //================================================================
<<<<<<< HEAD


=======
>>>>>>> 5331a81 (fix build script)
}
